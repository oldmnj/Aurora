// import launcher.base;


int main(void) {
    /*
    launcher::ConfigManager::Load();
    auto config = launcher::ConfigManager::Get();
    launcher::Logger::Initialize(config.logger);
    launcher::ConfigManager::Save("./config.json");
    launcher::Error err{launcher::ErrorCategory::None, launcher::ErrorCode::ParseError, "测试错误"};
    launcher::Logger::Info("Hello, {}!", "World");
    launcher::Logger::Error("{}", err.ToString());
    */
    return 0;
}
